# SonarQube Cloud project settings
# Replace the two placeholder values below with your actual SonarQube Cloud
# organization key and project key before enabling the sonar CI job.
sonar.organization=tholent
sonar.projectKey=jackdaw

sonar.sources=src
sonar.tests=tests
sonar.python.version=3.12

# Coverage report generated by pytest-cov in CI
sonar.python.coverage.reportPaths=coverage.xml
